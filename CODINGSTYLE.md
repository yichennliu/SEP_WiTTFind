# Coding style

`wittfind-web` uses the following conventions for javascript files:

* two spaces for indendation (no tabs!)
* c-based variable names and methods like `my_function` (instead of `MyFunction`)

This list will be extended in future!
